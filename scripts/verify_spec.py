"""Validate every worked example in docs/spec/examples against its JSON Schema.

Phase 1 deliverable. This stub exists so the script entry exists and the
intent is documented before the schemas are written.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("scripts/verify_spec.py: not yet implemented (Phase 1).", file=sys.stderr)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
